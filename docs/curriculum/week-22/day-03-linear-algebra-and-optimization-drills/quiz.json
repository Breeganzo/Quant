{
  "week": 22,
  "day_index": 3,
  "day": "Wed",
  "topic": "Linear algebra and optimization drills",
  "questions": [
    {
      "id": "q1",
      "difficulty": "basic",
      "question": "Explain linear algebra and optimization drills in plain language for a trading or risk audience.",
      "answer": "A strong answer defines linear algebra and optimization drills, gives one concrete workflow or market-facing decision example, and states one practical limitation that must be monitored.",
      "explanation": "This tests communication quality, not just memorized definitions."
    },
    {
      "id": "q2",
      "difficulty": "intermediate",
      "question": "Write the Portfolio Return formula/workflow from memory and define each symbol.",
      "answer": "A strong answer includes Portfolio Return exactly, explains each symbol, and states one caveat: Misaligned weight and return ordering.",
      "explanation": "This checks mathematical fluency and operational reliability.",
      "python_task": "Compute portfolio return and variance from a return matrix and covariance estimate.",
      "python_solution": "from pathlib import Path\nimport numpy as np\nimport pandas as pd\n\nmarket = pd.read_csv(Path(\"curriculum/datasets/real_market_prices.csv\"), parse_dates=[\"date\"])\nprices = market.pivot(index=\"date\", columns=\"symbol\", values=\"close\").dropna()\nreturns = prices.pct_change().dropna()\nw = np.array([0.4, 0.3, 0.2, 0.1])\nmu = returns.mean().values\ncov = returns.cov().values\nexp_ret = float(w @ mu)\nvar = float(w @ cov @ w)\nprint({\"expected_daily_return\": round(exp_ret, 6), \"daily_variance\": round(var, 8)})\n"
    },
    {
      "id": "q3",
      "difficulty": "intermediate",
      "question": "Give one realistic use case and one failure mode if this concept is misapplied.",
      "answer": "A strong answer uses one decision workflow such as: Fast multi-asset return aggregation.. Then it states one realistic failure mode: Misaligned weight and return ordering., and one detection check.",
      "explanation": "This evaluates transfer from theory to practical quant workflow."
    },
    {
      "id": "q4",
      "difficulty": "advanced",
      "question": "How would you validate data quality and implementation assumptions before trusting conclusions?",
      "answer": "Check schema consistency, missing values, temporal alignment, leakage risks, and sensitivity to stress windows. Then compare one metric across alternate assumptions or data sources.",
      "explanation": "This tests robustness discipline and implementation realism."
    }
  ]
}