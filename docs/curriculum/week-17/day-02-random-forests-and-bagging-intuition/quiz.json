{
  "week": 17,
  "day_index": 2,
  "day": "Tue",
  "topic": "Random forests and bagging intuition",
  "questions": [
    {
      "id": "q1",
      "difficulty": "basic",
      "question": "Explain random forests and bagging intuition in plain language for a trading or risk audience.",
      "answer": "A strong answer defines random forests and bagging intuition, gives one concrete workflow or market-facing decision example, and states one practical limitation that must be monitored.",
      "explanation": "This tests communication quality, not just memorized definitions."
    },
    {
      "id": "q2",
      "difficulty": "intermediate",
      "question": "Write the Bagging Predictor formula/workflow from memory and define each symbol.",
      "answer": "A strong answer includes Bagging Predictor exactly, explains each symbol, and states one caveat: Assuming bias also vanishes automatically.",
      "explanation": "This checks mathematical fluency and operational reliability.",
      "python_task": "Train a random forest classifier and report feature importances.",
      "python_solution": "from pathlib import Path\nimport pandas as pd\nfrom sklearn.ensemble import RandomForestClassifier\n\nmarket = pd.read_csv(Path(\"curriculum/datasets/real_market_prices.csv\"), parse_dates=[\"date\"])\nprices = market.pivot(index=\"date\", columns=\"symbol\", values=\"close\").dropna()\nreturns = prices.pct_change().dropna()\nX = returns.shift(1).dropna()\ny = (returns.loc[X.index, \"SPY\"] > 0).astype(int)\nmodel = RandomForestClassifier(n_estimators=200, random_state=7)\nmodel.fit(X, y)\nprint(pd.Series(model.feature_importances_, index=X.columns).sort_values(ascending=False))\n"
    },
    {
      "id": "q3",
      "difficulty": "intermediate",
      "question": "Give one realistic use case and one failure mode if this concept is misapplied.",
      "answer": "A strong answer uses one decision workflow such as: Variance reduction.. Then it states one realistic failure mode: Assuming bias also vanishes automatically., and one detection check.",
      "explanation": "This evaluates transfer from theory to practical quant workflow."
    },
    {
      "id": "q4",
      "difficulty": "advanced",
      "question": "How would you validate data quality and implementation assumptions before trusting conclusions?",
      "answer": "Check schema consistency, missing values, temporal alignment, leakage risks, and sensitivity to stress windows. Then compare one metric across alternate assumptions or data sources.",
      "explanation": "This tests robustness discipline and implementation realism."
    }
  ]
}